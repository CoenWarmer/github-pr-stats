{
  "name": "elastic-github-pr-stats",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc",
    "pr-timeline": "ts-node packages/pr-chart-cli/index.ts",
    "dev": "ts-node src/index.ts",
    "clean": "rm -rf dist",
    "prepare": "yarn build",
    "app:dev": "yarn workspace pr-stats-app dev",
    "app:build": "yarn workspace pr-stats-app build",
    "app:start": "yarn workspace pr-stats-app start",
    "inspect": "yarn workspace pr-chart-cli inspect"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@elastic/elasticsearch": "^8.10.0",
    "@octokit/rest": "22.0.0",
    "dotenv": "^16.3.1",
    "flat": "^6.0.1",
    "speedscope": "^1.23.1",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.17",
    "prettier": "^3.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
